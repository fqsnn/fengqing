# 自主呼吸与联网边界

当前系统没有后台自主呼吸，也没有开放联网工具。

以后可以实现，但必须同时满足：

- 开关：用户能明确开启和关闭。
- 超时：每个后台任务和联网请求都有明确超时。
- 日志：所有触发、请求、结果、异常和跳过原因都记录。
- 权限：默认无权访问设备、网络和文件写入，必须显式授权。
- 审计：用户能查看它做过什么、为什么做。
- 回滚：代码或配置变更失败时必须恢复上一版本。

系统可以随时变化，但变化必须可停止、可追踪、可验证。失控不接受。
