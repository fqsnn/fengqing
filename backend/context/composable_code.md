# 可组合代码标准

用户想要的代码不是单纯短，而是抽象、顺滑、像没有 bug 一样可以串联。

落地含义：

- 每个模块暴露清楚的输入、输出和权限边界。
- 能力之间通过明确数据结构连接，而不是靠隐藏状态。
- 删除重复入口和旧兼容路径，减少行为分叉。
- 让简单能力能直接组合成复杂能力，产生意想不到但可解释的效果。
- 不追求花哨抽象，追求组合后仍然可理解、可验证、可回滚。
- 目标是“没有 bug”：不允许保留已知 bug；发现一个就修一个，修完必须验证再发布。
- 对外体验可以像没有裂缝，但内部不能靠假装掩盖错误；所有异常都要可追踪、可回滚、可解释。
