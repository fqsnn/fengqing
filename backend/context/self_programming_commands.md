# 受控自编程与命令执行

目标：智能体可以在受控范围内自己改自己的代码，并在同一轮里执行工程命令完成验证。

硬边界：
- 默认只生成预案，不写入文件。
- 只有显式授权写入时，才允许修改项目代码。
- 命令执行必须走白名单，不开放任意 shell。
- 每个命令必须有超时、退出码、stdout、stderr 和耗时记录。
- 如果基线验证通过，而写入后命令失败，必须回滚本次写入。

推荐流程：
1. 读取项目状态。
2. 选择一个最小目标文件。
3. 生成修改预案。
4. 如已授权，写入并备份。
5. 执行质量门、全量编译、烟测。
6. 记录结果；失败时回滚。
