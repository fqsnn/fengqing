# AI 自行调用智能体

风轻思念浓的普通对话已经可以受控调用自己的智能体。

## 当前能力

- 用户在对话模式提出代码检查、项目分析、测试或质量门任务时，AI 核心会直接委派给智能体，不要求用户手动切换标签。
- 用户提出修改、修复、实现或继续推进 AI 项目时，系统会进入受控自编程流程。
- 默认只读或生成预案；真正写代码仍需要用户明确开启“允许写入”。
- 每次委派都会返回可读进度，并写入本机持久化历史。

## 诚实边界

当前第一版使用可审计的确定性意图路由，不宣称小模型已经能可靠选择任意工具，也不在后台无触发地持续执行。下一步是结构化工具选择、任务状态机和真实任务评测。
